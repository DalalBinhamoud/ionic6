import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.devdactic.identityapp',
  appName: 'identityVault',
  webDir: 'www',
  bundledWebRuntime: false,
}

export default config
