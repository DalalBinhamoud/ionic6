import { SteckySegmentDirective } from './stecky-segment.directive';

describe('SteckySegmentDirective', () => {
  it('should create an instance', () => {
    const directive = new SteckySegmentDirective();
    expect(directive).toBeTruthy();
  });
});
